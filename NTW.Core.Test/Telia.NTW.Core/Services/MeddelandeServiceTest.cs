using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace Telia.NTW.Core.Test.Core.Services
{
	[TestClass]
	public class MeddelandeServiceTest
	{
		[TestMethod]
		public void Create_Single()
		{
			//var target = new X();

			// TODO use AutoFixture

			//var mockSet = new Mock<DbSet<Meddelande>>();

			//var mockContext = new Mock<Context>();
			//mockContext.Setup(m => m.Meddelande).Returns(mockSet.Object);

			//meddelandeService.CreateMessage(new Meddelande {
			//	From = DateTime.Today.AddDays(-1),
			//	To = DateTime.Today.AddDays(1),
			//	Text = ...
			//});
		}
	}
}
